export interface Artiste{
    id: number;
  title: {
    rendered: string;
  };
  acf: {
    nom: string;
    date: string;
    scene: string;
    horaire: string;
    biographie: string;
  };
}